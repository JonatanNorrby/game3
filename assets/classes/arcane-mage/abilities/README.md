# Arcane Mage ability icons

These SVGs are temporary prototype icons.

Keep production ability art in this folder with the same filenames so the class configuration can continue referencing stable asset paths without changing gameplay code.
